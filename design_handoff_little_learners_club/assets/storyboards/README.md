# assets/storyboards/

Exported frames for each world's **six-beat module introduction** (On the map → Travel → Landmark
wakes → One small moment → Title reveal → Into the path).

- **Status:** *placeholder → illustrator* (category 2 in `../../docs/design/ASSET_MANIFEST.md`).
  Generated storyboard **thumbnails already exist** inside `../prototypes/Module Worlds.dc.html`
  (one row of six beats per world) — this folder is where exported frames belong once produced.
- **Governed by:** `../../docs/design/MODULE_WORLDS.md §1` (the formula, timing tokens per beat, the
  ≤ 8s / skippable / fully-narrated rule, and the reduced-motion five-still-frame substitution).

_No exported storyboard files are included in this bundle yet; view them live in the prototype._
